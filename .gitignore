# === Internal development ecosystem — not part of the product (ADR-007) ===
_hilo/
_patron/
Documentos_Base/
ecosystem.config.json

# Agent tooling & workspace config (development tooling, not product)
AGENTS.md
GEMINI.md
.agents/
.gemini/

# Internal project documentation & specs (Spanish)
BRIEF_STRIPBOARD.md
RUNBOOK.md
EVOLUTIVOS.md

# === .NET ===
bin/
obj/
artifacts/
*.user
.vs/
*.trx
coverage.cobertura.xml

# === Python (agents/) ===
__pycache__/
*.pyc
.venv/
venv/

# === Secrets and environment — live in Secret Manager ===
.env
*.env
appsettings.*.local.json
.secrets/

# === OS ===
.DS_Store
Thumbs.db

# Mutation testing reports (dotnet stryker). Regenerated on demand; the score that
# matters is recorded in adr/ADR-022 and the README.
StrykerOutput/
